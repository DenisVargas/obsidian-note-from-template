/**
 * UI type definitions for the FromTemplate plugin.
 * Shared types used across modal and utility components.
 */

export type SubcontrolParams = {
    title: string;
    content?: string;
    description?: string;
    labelCls?: string[];
    contentCls?: string[];
    rowCls?: string[];
    keyDisplay?: string;
};
